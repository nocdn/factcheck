services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: factcheck:local
    env_file:
      - path: .env
        required: false
    environment:
      NODE_ENV: production
      PORT: ${PORT:-7110}
    ports:
      - "${PORT:-7110}:${PORT:-7110}"
    restart: always
